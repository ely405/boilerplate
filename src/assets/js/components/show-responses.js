'use strict';

const createShowReponse = (responses)=>{
    
     
}

$('#all-topic-container').on('click', 'button', function(e) {
    console.log('delegate');
    let id = $(this).attr('data-id');
});
